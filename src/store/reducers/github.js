const initialState = {
    searchText: '',
    profile: null,
    repositories: null
}

const github = ( state = initialState, action) => {

    switch(action.type){


        default:
            return state;
    }
}


export default github;